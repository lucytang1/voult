user 
id 
email
user_key
vault_id (1 to 1) Vault(id)

vault 
id 
vault


AUTH: 
what do i need for this? 
a session store (DB(Sqlite))
interface to the store (CRUD operations on the session store)
a middleware that is wired on the app which preprocesses a request (does authorization and authentication checks)

